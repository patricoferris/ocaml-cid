(test
 (name test)
 (modules test)
 (libraries alcotest cid))

; Enable once we're 4.10+
; (test
;  (name irmin_cid)
;  (modules irmin_cid)
;  (libraries lwt.unix irmin irmin-test irmin.mem cid))

; (mdx
;  (deps irmin_cid.exe)
;  (package cid))
